# llmLibrarian: local RAG + Ollama
chromadb>=0.4.0
ollama>=0.1.0
PyYAML>=6.0
# PDF (fitz)
pymupdf>=1.23.0
# DOCX
python-docx>=1.0.0

# Optional: xlsx/pptx text extraction (otherwise metadata-only chunk with content_extracted=0)
# openpyxl>=3.0.0
# python-pptx>=0.6.0

# Optional: reranker (set LLMLIBRARIAN_RERANK=1)
# sentence-transformers>=2.2.0
